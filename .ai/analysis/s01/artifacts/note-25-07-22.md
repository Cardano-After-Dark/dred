# Note

## VPS
- docker
- redis (docker)
- DRED wraps an API to command redis

## Neighborhoods
- one machine can run multiple neighborhoods (perhaps)
- each neighborhood should run on a separate nodejs instance connected to a redis instance one haproxy

So, we may think of a container for a neighborhood with 
- a nodejs instance
- a redis instance
- an haproxy

Currently, we are much more time sensitive than in the past. 
Timely results now are more important than accurate ones 

objective: a cluster of DRED servers that we can demonstrate quickly, possibly with ssl handshake
