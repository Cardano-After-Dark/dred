I am not sure the provided fix works, so I need to stash it and reconsider it from scratch . Here is what I need to do: 
1. after each change, I will need to deploy the changes on the US server by running `make setup-dred us` from the preprod server. This should deploy the current version of dred from the current branch and start the server on the VPS. Note that for this depoloy, the environment variables from proprod/config/us.env are read and passed to the VPS which runs DRED with those variables. Also note that from the preprod folder we can run `make test us` to verify the status of the server. 
2. When the code is deployed and running on the VPS, first we need to check thad the remote dred is running and which channels are available. Then, we need to have as client that can connect to the remote server and can send a message to that remote server on a specified channel. To do so, we have a script, in the scripts directory: the interactive `test-client.js`. This substantially connects to the remote VPS, checks a channel is available and allow the user to send messages on that channel. 
3. Launch the DRED server locally with the command
  `LOGGING=default:debug,replicant:trace,replicator:trace pnpm exec node dist/dredServer.mjs | pnpm exec pino-pretty` . This log level ensures the replication and replicant are logging everything. When Launching, in the output I expect to see the local server is connecting to other servers (using Discovery); among those I expect to see the US server, deployed at 1. At this point, since the local server is connected to the remote US dred, I expect that sending a client message, e.g. on the channel news, should result in the local server replicating the message sent on the remote-us server. This information should be visible on the console at trace level. 
4. Iterate by asking user input until the replication works as intended. Ask the user for any doubt. For the client connection, take a look at how the Replicator and Replicant are creating Clients. When changing the code consider carefully the environment variables both for the local DRED server and for the remote one. Feel free to change the interactive test client, but be careful when changing other central parts of the code. 