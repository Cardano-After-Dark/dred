# Context

The interaction should havew a very high signal/noise ratio. The code should be minimal and clear. If there is any doubt, please interact with the user. 

Currently, we are in the phase in which we are testing the replication of messages between servers. When this is done we will proceed by implementing an application using DRED. 